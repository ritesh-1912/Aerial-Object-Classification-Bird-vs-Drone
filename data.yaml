# YOLOv8 dataset config (run `python convert_coco_to_yolo.py` first to build layout).
# Paths are relative to the project root when you run training from there.
path: drones and birds/yolo_dataset
train: train/images
val: valid/images
test: test/images
nc: 2
names:
  0: Bird
  1: drone
